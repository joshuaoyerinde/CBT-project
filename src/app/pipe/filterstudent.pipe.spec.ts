import { FilterstudentPipe } from './filterstudent.pipe';

describe('FilterstudentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterstudentPipe();
    expect(pipe).toBeTruthy();
  });
});
