import { PipQuestionPipe } from './pip-question.pipe';

describe('PipQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new PipQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
