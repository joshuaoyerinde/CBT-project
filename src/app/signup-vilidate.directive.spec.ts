import { SignupVilidateDirective } from './signup-vilidate.directive';

describe('SignupVilidateDirective', () => {
  it('should create an instance', () => {
    const directive = new SignupVilidateDirective();
    expect(directive).toBeTruthy();
  });
});
